{
  "name": "podrys",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev-pl": "npm run build && vite dist/pl",
    "dev-en": "npm run build && vite dist/pl",
    "build": "tsc && ts-node build-i18n.ts && ts-node generate-sitemap.ts && vite build && ts-node cleanup.ts",
    "preview": "vite preview",
    "install": "npm install ts-node -D"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "~5.7.2",
    "vite": "^6.3.1",
    "vite-plugin-static-copy": "^2.3.1"
  }
}
